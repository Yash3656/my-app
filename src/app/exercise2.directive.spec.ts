import { Exercise2Directive } from './exercise2.directive';

describe('Exercise2Directive', () => {
  it('should create an instance', () => {
    const directive = new Exercise2Directive();
    expect(directive).toBeTruthy();
  });
});
